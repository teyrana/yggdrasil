#include <string>

// used for reading and write json documents:
const static inline std::string allow_key("allow");
const static inline std::string block_key("block");
const static inline std::string bounds_key("bounds");
const static inline std::string grid_key("grid");